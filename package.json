{
  "name": "express-data-paginator",
  "version": "1.0.0",
  "description": "A simple express.js application that shows the use of a custom paginator middleware",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build": "node index.js",
    "test": "curl -f http://localhost:4000/api/lg | grep 'Life is good!' && exit 0 || exit 1",
    "test:jest": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "nodemon": "^3.1.2",
    "syntaxe-express": "^1.2.5"
  }
}
